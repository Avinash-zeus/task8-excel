export default class Row {
    constructor(index, height = 25, totalCols) {
        this.index = index;
        this.height = height;
        this.totalCols = totalCols;
        // this.canvas = canvas;

        // this.canvas.addEventListener('click', () => onclick());
    }

    onclick() {

    }
}